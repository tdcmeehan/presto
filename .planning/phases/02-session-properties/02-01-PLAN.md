---
phase: 02-session-properties
plan: 01
type: execute
---

<objective>
Add session properties for dynamic partition pruning enablement and configuration.

Purpose: Allow users to enable/disable dynamic partition pruning per session and configure the maximum wait time for filter availability before proceeding with split scheduling.
Output: Two new session properties accessible via SET SESSION and visible in SHOW SESSION.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summary (dependency):
@.planning/phases/01-spi-foundation/01-01-SUMMARY.md

# Key files to modify:
@presto-main-base/src/main/java/com/facebook/presto/sql/analyzer/FeaturesConfig.java
@presto-main-base/src/main/java/com/facebook/presto/SystemSessionProperties.java

**Tech stack available:** Standard Presto FeaturesConfig + SystemSessionProperties pattern
**Established patterns:**
- FeaturesConfig: private field, getter, @Config-annotated setter
- SystemSessionProperties: constant, PropertyMetadata in constructor, static accessor method
- Duration properties use VARCHAR type with Duration.valueOf parser

**Constraining decisions:**
- Phase 1: Used com.facebook.airlift.units.Duration (presto-spi dependency) - use same Duration class here
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dynamic partition pruning fields and methods to FeaturesConfig</name>
  <files>presto-main-base/src/main/java/com/facebook/presto/sql/analyzer/FeaturesConfig.java</files>
  <action>
Add two new fields near the existing dynamic filtering fields (around line 173):
- `private boolean dynamicPartitionPruningEnabled;` (default false - conservative opt-in)
- `private Duration dynamicPartitionPruningMaxWaitTime = new Duration(2, SECONDS);` (conservative default)

Add getter and setter methods near the existing dynamic filtering methods (around line 1555):
1. `isDynamicPartitionPruningEnabled()` getter
2. `setDynamicPartitionPruningEnabled(boolean)` setter with `@Config("dynamic-partition-pruning-enabled")`
3. `getDynamicPartitionPruningMaxWaitTime()` getter
4. `setDynamicPartitionPruningMaxWaitTime(Duration)` setter with `@Config("dynamic-partition-pruning-max-wait-time")`

Follow the exact pattern used by existing dynamic filtering properties. Do NOT add @LegacyConfig since this is a new feature.
  </action>
  <verify>./mvnw compile -pl presto-main-base -DskipTests -q</verify>
  <done>FeaturesConfig compiles with new fields and methods, no errors</done>
</task>

<task type="auto">
  <name>Task 2: Add session properties to SystemSessionProperties</name>
  <files>presto-main-base/src/main/java/com/facebook/presto/SystemSessionProperties.java</files>
  <action>
1. Add constants near ENABLE_DYNAMIC_FILTERING (around line 222):
   - `public static final String DYNAMIC_PARTITION_PRUNING_ENABLED = "dynamic_partition_pruning_enabled";`
   - `public static final String DYNAMIC_PARTITION_PRUNING_MAX_WAIT_TIME = "dynamic_partition_pruning_max_wait_time";`

2. Add PropertyMetadata entries in constructor after the DYNAMIC_FILTERING_RANGE_ROW_LIMIT_PER_DRIVER property (around line 1238):
   - Boolean property for enabled (use `booleanProperty` helper)
   - Duration property for max wait time (use `new PropertyMetadata<>` with VARCHAR type, Duration class, Duration.valueOf parser, Duration::toString - follow QUERY_MAX_EXECUTION_TIME pattern at line 604)

3. Add static accessor methods near the existing dynamic filtering accessors (around line 2850):
   - `public static boolean isDynamicPartitionPruningEnabled(Session session)`
   - `public static Duration getDynamicPartitionPruningMaxWaitTime(Session session)`

Property descriptions:
- enabled: "Enable dynamic partition pruning during split scheduling"
- max_wait_time: "Maximum time to wait for dynamic partition pruning filter before proceeding with split scheduling"
  </action>
  <verify>./mvnw compile -pl presto-main-base -DskipTests -q</verify>
  <done>SystemSessionProperties compiles with new properties and accessors</done>
</task>

<task type="auto">
  <name>Task 3: Verify session properties work end-to-end</name>
  <files></files>
  <action>
Run a quick integration test to verify the properties are registered and accessible:
1. Build the module: `./mvnw install -pl presto-main-base -DskipTests -q`
2. Run the TestSystemSessionProperties test to ensure no regressions
3. Grep for the new properties in compiled output to verify registration
  </action>
  <verify>./mvnw test -pl presto-main-base -Dtest=TestSystemSessionProperties -q</verify>
  <done>TestSystemSessionProperties passes, no regressions</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `./mvnw compile -pl presto-main-base -DskipTests -q` succeeds
- [ ] `./mvnw test -pl presto-main-base -Dtest=TestSystemSessionProperties -q` passes
- [ ] New properties follow Presto naming conventions (snake_case for session property names)
- [ ] Default values are conservative (disabled by default, reasonable wait time)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Session properties match roadmap specification:
  - `dynamic_partition_pruning_enabled` (boolean, default false)
  - `dynamic_partition_pruning_max_wait_time` (Duration, default 2s)
  </success_criteria>

<output>
After completion, create `.planning/phases/02-session-properties/02-01-SUMMARY.md`
</output>
