---
phase: 09-unit-testing
plan: 02
type: execute
---

<objective>
Create unit tests for DynamicFilterResponse JSON serialization in presto-main module.

Purpose: Ensure the HTTP transport layer for dynamic filters works correctly with proper JSON round-trip serialization.
Output: TestDynamicFilterResponse.java with JSON codec tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/05-coordinator-filter-fetching/05-01-SUMMARY.md

# Source file to test:
@presto-main/src/main/java/com/facebook/presto/server/remotetask/DynamicFilterResponse.java

# Existing test patterns:
@presto-main/src/test/java/com/facebook/presto/server/remotetask/TestHttpRemoteTaskConnectorCodec.java (for JsonCodec test patterns)

**Tech stack available:** TestNG, JsonCodec, TupleDomain
**Established patterns:** JsonCodec.jsonCodec(), codec.toJson()/fromJson() round-trip tests

**Constraining decisions:**
- Phase 5: DynamicFilterResponse wrapper for cleaner JsonCodec binding
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestDynamicFilterResponse</name>
  <files>presto-main/src/test/java/com/facebook/presto/server/remotetask/TestDynamicFilterResponse.java</files>
  <action>
Create TestDynamicFilterResponse.java with these test cases:

1. testEmptyFilters - DynamicFilterResponse with empty filter map serializes/deserializes correctly
2. testSingleFilter - Response with one filter (filterId -> TupleDomain) round-trips correctly
3. testMultipleFilters - Response with multiple filters round-trips correctly
4. testVersionField - Version field serializes/deserializes correctly
5. testTupleDomainAll - Filter with TupleDomain.all() serializes correctly
6. testTupleDomainNone - Filter with TupleDomain.none() serializes correctly
7. testTupleDomainWithDomains - Filter with actual column domains round-trips correctly

Use JsonCodec<DynamicFilterResponse> from com.facebook.airlift.json.JsonCodec.
Create codec with: JsonCodec.jsonCodec(DynamicFilterResponse.class) - note this requires proper ObjectMapperProvider setup.

Alternative approach if codec creation is complex: Use ObjectMapper directly with the Jackson annotations already on DynamicFilterResponse.

Follow existing TestNG patterns. Verify round-trip: assertEquals(original, codec.fromJson(codec.toJson(original)))
  </action>
  <verify>./mvnw test-compile -pl presto-main -Dair.check.skip-all=true succeeds</verify>
  <done>TestDynamicFilterResponse.java exists with 7 test methods covering JSON serialization</done>
</task>

<task type="auto">
  <name>Task 2: Run and verify tests pass</name>
  <files>presto-main/src/test/java/com/facebook/presto/server/remotetask/TestDynamicFilterResponse.java</files>
  <action>
Run the new test class to verify all tests pass:
./mvnw test -pl presto-main -Dtest=TestDynamicFilterResponse -Dair.check.skip-all=true

If any tests fail due to JsonCodec setup complexity, simplify to use ObjectMapper directly for JSON round-trip testing.
  </action>
  <verify>All tests pass with no failures</verify>
  <done>TestDynamicFilterResponse runs successfully with all tests passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TestDynamicFilterResponse.java compiles without errors
- [ ] All test methods pass
- [ ] JSON serialization round-trip works for empty, single, and multiple filters
</verification>

<success_criteria>

- TestDynamicFilterResponse.java created
- All tests pass
- JSON serialization verified for DynamicFilterResponse
</success_criteria>

<output>
After completion, create `.planning/phases/09-unit-testing/09-02-SUMMARY.md`
</output>
