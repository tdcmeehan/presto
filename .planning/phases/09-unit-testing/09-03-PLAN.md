---
phase: 09-unit-testing
plan: 03
type: execute
---

<objective>
Run full dynamic filter test suite across all three modules and verify Phase 9 completion criteria.

Purpose: Validate all unit tests pass and coverage requirements are met for Phase 9 completion.
Output: Verified test suite passing, Phase 9 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan summaries:
@.planning/phases/09-unit-testing/09-01-SUMMARY.md
@.planning/phases/09-unit-testing/09-02-SUMMARY.md

**Phase 9 verification criteria (from ROADMAP.md):**
- [ ] All new classes have >80% line coverage
- [ ] Edge cases tested: empty filter, timeout, single partition, many partitions
- [ ] `./mvnw test -pl presto-main-base,presto-main,presto-iceberg` passes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run complete dynamic filter test suite</name>
  <files>N/A - verification task</files>
  <action>
Run all dynamic filter related tests across the three modules:

./mvnw test -pl presto-main-base,presto-main,presto-iceberg -Dtest="*DynamicFilter*,*PartitionPredicate*" -Dair.check.skip-all=true

This should include:
- TestCoordinatorDynamicFilter (presto-main-base)
- TestDynamicFilterService (presto-main-base)
- TestDynamicFilterResponse (presto-main)
- TestIcebergPartitionPredicate (presto-iceberg)

If any tests fail, document the failures but do not attempt fixes in this plan (those would be separate issues).
  </action>
  <verify>Test command completes, capture pass/fail counts</verify>
  <done>Full test suite executed with results documented</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 9 completion criteria</name>
  <files>N/A - verification task</files>
  <action>
Check Phase 9 verification criteria from ROADMAP.md:

1. **Edge cases tested:** Verify tests exist for:
   - Empty filter (TupleDomain.all())
   - Timeout behavior (graceful degradation)
   - Single partition
   - Many partitions (multiple columns)

2. **Test suite passes:** Confirm ./mvnw test -pl presto-main-base,presto-main,presto-iceberg passes

3. **Coverage:** Note that >80% line coverage is a guideline - the critical path tests (filter merging, timeout, edge cases) are the priority per CONTEXT.md.

Document which criteria are met and any gaps.
  </action>
  <verify>All verification criteria assessed</verify>
  <done>Phase 9 verification criteria documented with status</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Full test suite executed
- [ ] Test results documented
- [ ] Phase 9 verification criteria assessed
</verification>

<success_criteria>

- All dynamic filter tests pass
- Phase 9 verification criteria met or gaps documented
- Phase 9 complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-unit-testing/09-03-SUMMARY.md` with:
- Test execution results
- Phase 9 verification status
- Mark Phase 9 complete if all criteria met
</output>
