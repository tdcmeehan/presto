---
phase: 09-unit-testing
plan: 01
type: execute
---

<objective>
Create unit tests for IcebergUtil.partitionMatchesPredicate() to validate partition pruning logic.

Purpose: Ensure the partition predicate matching correctly handles all edge cases - the critical path for dynamic filter effectiveness in Iceberg.
Output: TestIcebergPartitionPredicate.java with comprehensive edge case coverage.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/07-iceberg-integration/07-01-SUMMARY.md
@.planning/phases/03-coordinator-filter-infrastructure/03-03-SUMMARY.md

# Source files to test:
@presto-iceberg/src/main/java/com/facebook/presto/iceberg/IcebergUtil.java (lines 646-727: partitionMatchesPredicate, convertPartitionValue)

# Existing test patterns:
@presto-iceberg/src/test/java/com/facebook/presto/iceberg/TestIcebergSplitManager.java (for test structure patterns)
@presto-main-base/src/test/java/com/facebook/presto/execution/scheduler/TestCoordinatorDynamicFilter.java (for TupleDomain test patterns)

**Tech stack available:** TestNG, TupleDomain, IcebergColumnHandle, HivePartitionKey
**Established patterns:** TestNG @Test methods, TupleDomain.withColumnDomains(), Domain.singleValue()

**Constraining decisions:**
- Phase 7: Graceful degradation - proceed without filtering on conversion failure
- Phase 7: Convert TupleDomain<ColumnHandle> to IcebergColumnHandle via transform
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestIcebergPartitionPredicate</name>
  <files>presto-iceberg/src/test/java/com/facebook/presto/iceberg/TestIcebergPartitionPredicate.java</files>
  <action>
Create TestIcebergPartitionPredicate.java with these test cases for IcebergUtil.partitionMatchesPredicate():

1. testFilterConstraintIsAll - TupleDomain.all() should return true (no filtering)
2. testFilterConstraintIsNone - TupleDomain.none() should return false (filter everything)
3. testEmptyPartitionKeys - Empty partition map should return true (unpartitioned table)
4. testPartitionValueMatchesDomain - Partition value "2024" matches Domain containing 2024 returns true
5. testPartitionValueNotInDomain - Partition value "2024" not in Domain {2023, 2025} returns false
6. testNullPartitionValueWithNullAllowed - Null partition value with domain.isNullAllowed()=true returns true
7. testNullPartitionValueWithNullNotAllowed - Null partition value with domain.isNullAllowed()=false returns false
8. testNonPartitionColumnInFilter - Filter on non-partition column should be skipped (return true)
9. testMultiplePartitionColumns - Filter with multiple partition columns, all must match

Use IcebergColumnHandle with proper ID, name, and type. Use HivePartitionKey for partition values.
Follow existing TestNG patterns from the codebase. Use static imports for TupleDomain, Domain, etc.

DO NOT use mocking frameworks - create real IcebergColumnHandle and HivePartitionKey instances.
  </action>
  <verify>./mvnw test-compile -pl presto-iceberg -Dair.check.skip-all=true succeeds</verify>
  <done>TestIcebergPartitionPredicate.java exists with 9 test methods covering all edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Run and verify tests pass</name>
  <files>presto-iceberg/src/test/java/com/facebook/presto/iceberg/TestIcebergPartitionPredicate.java</files>
  <action>
Run the new test class to verify all tests pass:
./mvnw test -pl presto-iceberg -Dtest=TestIcebergPartitionPredicate -Dair.check.skip-all=true

If any tests fail, analyze the failure and fix the test or identify if it reveals a bug in the implementation.
  </action>
  <verify>All 9 tests pass with no failures</verify>
  <done>TestIcebergPartitionPredicate runs successfully with all tests passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TestIcebergPartitionPredicate.java compiles without errors
- [ ] All 9 test methods pass
- [ ] Tests cover: isAll, isNone, empty partitions, matching, non-matching, null values, non-partition columns, multiple columns
</verification>

<success_criteria>

- TestIcebergPartitionPredicate.java created with 9 test methods
- All tests pass
- Edge cases covered per CONTEXT.md requirements
</success_criteria>

<output>
After completion, create `.planning/phases/09-unit-testing/09-01-SUMMARY.md`
</output>
