---
phase: 03-coordinator-filter-infrastructure
plan: 03
type: execute
subsystem: coordinator
---

<objective>
Create unit tests for CoordinatorDynamicFilter and DynamicFilterService.

Purpose: Verify filter accumulation logic, TupleDomain merging, and service registration/lookup work correctly.
Output: Test classes with comprehensive coverage of coordinator filter infrastructure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans in this phase:
@.planning/phases/03-coordinator-filter-infrastructure/03-01-PLAN.md
@.planning/phases/03-coordinator-filter-infrastructure/03-02-PLAN.md

# Key test files to reference:
@presto-main-base/src/test/java/com/facebook/presto/sql/planner/TestLocalDynamicFilter.java
@presto-main-base/src/test/java/com/facebook/presto/sql/planner/TestLocalDynamicFiltersCollector.java

**Established patterns:**
- TestNG framework (@Test annotations)
- assertEquals, assertFalse, assertTrue from testng.Assert
- ImmutableMap, ImmutableSet for test data
- ExecutionException, InterruptedException for future tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestCoordinatorDynamicFilter</name>
  <files>presto-main-base/src/test/java/com/facebook/presto/execution/scheduler/TestCoordinatorDynamicFilter.java</files>
  <action>
Create unit test class following TestLocalDynamicFilter patterns.

Test cases:
1. testSimpleSinglePartition - single partition completes filter immediately
2. testMultiplePartitions - N partitions, filter completes after Nth partition
3. testColumnWiseUnion - verify TupleDomain.columnWiseUnion() correctly merges discrete values
4. testEmptyPartition - partition with TupleDomain.all() doesn't restrict filter
5. testNonePartition - partition with TupleDomain.none() results in none filter
6. testIsComplete - verify isComplete() returns correct state before/after completion
7. testGetColumnsCovered - verify columns returned match constructor input
8. testGetWaitTimeout - verify timeout returned matches constructor input

Use test ColumnHandle implementations (can create simple inner class TestColumnHandle).
Use Duration from com.facebook.airlift.units.Duration.
Use INTEGER and BIGINT types for Domain values.
  </action>
  <verify>./mvnw test -pl presto-main-base -Dtest=TestCoordinatorDynamicFilter -Dair.check.skip-all=true</verify>
  <done>All test cases pass, >80% line coverage on CoordinatorDynamicFilter</done>
</task>

<task type="auto">
  <name>Task 2: Create TestDynamicFilterService</name>
  <files>presto-main-base/src/test/java/com/facebook/presto/execution/scheduler/TestDynamicFilterService.java</files>
  <action>
Create unit test class for DynamicFilterService.

Test cases:
1. testRegisterAndGet - register filter, verify getFilter returns it
2. testGetNonexistent - getFilter for unknown query/filter returns empty Optional
3. testHasFilter - verify hasFilter returns correct boolean
4. testRemoveFiltersForQuery - register multiple, remove by query, verify all gone
5. testMultipleQueries - register filters for different queries, verify isolation
6. testMultipleFiltersPerQuery - register multiple filters per query, verify all accessible

Use mock CoordinatorDynamicFilter instances (can use simple constructor with test values).
Use QueryId.valueOf("test_query_1") pattern for test query IDs.
  </action>
  <verify>./mvnw test -pl presto-main-base -Dtest=TestDynamicFilterService -Dair.check.skip-all=true</verify>
  <done>All test cases pass, >80% line coverage on DynamicFilterService</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite verification</name>
  <files>N/A</files>
  <action>
Run both test classes together and verify no regressions in existing dynamic filter tests.

Commands:
1. ./mvnw test -pl presto-main-base -Dtest=TestCoordinatorDynamicFilter,TestDynamicFilterService -Dair.check.skip-all=true
2. ./mvnw test -pl presto-main-base -Dtest=TestLocalDynamicFilter -Dair.check.skip-all=true (verify no regression)
  </action>
  <verify>./mvnw test -pl presto-main-base -Dtest=Test*DynamicFilter* -Dair.check.skip-all=true</verify>
  <done>All dynamic filter tests pass, no regressions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./mvnw test -pl presto-main-base -Dtest=TestCoordinatorDynamicFilter -Dair.check.skip-all=true` passes
- [ ] `./mvnw test -pl presto-main-base -Dtest=TestDynamicFilterService -Dair.check.skip-all=true` passes
- [ ] Existing TestLocalDynamicFilter tests still pass
- [ ] Edge cases tested: empty filter, none filter, timeout handling
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No test failures
- Phase 3 complete - coordinator filter infrastructure validated
</success_criteria>

<output>
After completion, create `.planning/phases/03-coordinator-filter-infrastructure/03-03-SUMMARY.md` with:
- Test coverage summary
- Any issues encountered
- Phase 3 completion status
</output>
